/**
 * 插件入口文件
 * 统一导出所有插件
 */

export { cameraManager, CameraManager } from './camera-plugin.uts'
export type { 
  CameraConfig, 
  RecordingConfig, 
  RecordingStatus, 
  PerformanceData 
} from './camera-plugin.uts'
